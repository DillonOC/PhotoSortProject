package metadata;

import java.io.File;
import java.util.Calendar;

public class DateExtractor {
    
    public Calendar extractDate(File photo) {
        // Stub
        Calendar extractedDate = Calendar.getInstance();
        return extractedDate;
    }
}
